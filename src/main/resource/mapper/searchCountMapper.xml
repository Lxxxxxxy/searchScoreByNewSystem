<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">
    <update id="add">
        update searchcount set count=count+1
    </update>

    <select id="selectCount" resultType="entity.searchCount">
        select * from searchcount
    </select>
</mapper>